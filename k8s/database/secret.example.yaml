---
# TEMPLATE ONLY - do not commit a populated copy.
#
#   kubectl create secret generic mysql-secret -n library \
#     --from-literal=MYSQL_ROOT_PASSWORD='<choose-a-strong-password>'
#
# MYSQL_ROOT_PASSWORD must match DB_PASSWORD in app-secret.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: library
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: REPLACE_ME
