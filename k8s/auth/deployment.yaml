apiVersion: apps/v1
kind: Deployment

metadata:
  name: auth-service
  namespace: library
  labels:
    app: auth-service
    tier: backend
    project: digital-library

spec:
  replicas: 2

  selector:
    matchLabels:
      app: auth-service

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  template:
    metadata:
      labels:
        app: auth-service
        tier: backend
        project: digital-library

    spec:
      containers:
      - name: auth-service

        image: devsecops1acr.azurecr.io/auth-service:latest

        imagePullPolicy: Always

        ports:
        - containerPort: 5001

        envFrom:
        - configMapRef:
            name: app-config

        - secretRef:
            name: app-secret

        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"

          limits:
            cpu: "500m"
            memory: "512Mi"

        livenessProbe:
          tcpSocket:
            port: 5001
          initialDelaySeconds: 30
          periodSeconds: 15

        readinessProbe:
          tcpSocket:
            port: 5001
          initialDelaySeconds: 10
          periodSeconds: 10
